# Define the custom order of derive attributes.
order = ["Debug", "Default", "Clone", "Copy" , "PartialEq", "Eq", "PartialOrd", "Ord", "Hash", "...", "Component", "Serialize", "Deserialize", "Reflect", "Facet"]

# Preserve the original order for unspecified derive attributes
preserve = true
