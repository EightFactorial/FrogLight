[package]
name = "mc-rs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["EightFactorial"]
homepage = "https://github.com/EightFactorial/MC-RS"
repository = "https://github.com/EightFactorial/MC-RS"
readme = "README.md"

[workspace]
resolver = "2"
members = [
    "crates/mc-rs-core",
    "crates/mc-rs-interface",
    "crates/mc-rs-macros",
    "crates/mc-rs-network",
    "crates/mc-rs-protocol",
    "crates/tools/classfile-rs",
    "crates/tools/mc-rs-extract",
    "crates/tools/mc-rs-generate",
]

[workspace.dependencies]
azalea-chat = { version = "0.8", default-features = false }
azalea-nbt = { version = "0.8", git = "https://github.com/EightFactorial/azalea.git", branch = "nbt", default-features = false }
bevy = "0.12"
bevy_rapier3d = { version = "0.23", features = ["parallel"] }
compact_str = { version = "0.7", features = ["serde", "smallvec"] }
derive_more = "0.99"
futures-lite = { version = "2.0", features = ["futures-io"] }
itertools = "0.11"
log = { version = "0.4", features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0" }
smallvec = { version = "1.11", features = ["const_generics", "const_new", "union", "write"] }
strum = { version = "0.25", features = ["derive"] }
uuid = "1.5"

[profile.dev]
incremental = true
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.dev.package.mc-rs-network]
opt-level = 3

[profile.dev.package.mc-rs-protocol]
opt-level = 3

[profile.dev.package.mc-rs-interface]
opt-level = 1

[profile.dev.build-override]
opt-level = 3

[profile.release]
incremental = false
opt-level = 3
strip = true
lto = true

[profile.release.build-override]
opt-level = 3

[dependencies]
bevy = { workspace = true }
mc-rs-interface = { version = "0.1.0", path = "crates/mc-rs-interface" }
mc-rs-network = { version = "0.1.0", path = "crates/mc-rs-network" }

[features]
default = ["simd"]

# Enable debug features
debug = ["mc-rs-interface/debug", "mc-rs-network/debug"]
# Enable bevy-rapier debug features
debug_rapier = ["mc-rs-interface/debug_rapier"]

# Enable wayland features
wayland = ["mc-rs-interface/wayland"]

# Enable simd features
simd = ["mc-rs-interface/simd", "mc-rs-network/simd"]
# !! Currently broken on the latest nightly !!
simd_advanced = ["mc-rs-interface/simd_advanced", "mc-rs-network/simd_advanced"]

