[package]
edition = { workspace = true }
include = { workspace = true }
license = { workspace = true }
name = "froglight-block"
readme = { workspace = true }
repository = { workspace = true }
rust-version = { workspace = true }
version = { workspace = true }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[lints]
workspace = true

# --- Dependencies ---

[dev-dependencies]
criterion = { features = ["default", "html_reports"], workspace = true }
rand = { features = ["default"], workspace = true }

[dependencies]
froglight-common = { features = ["glam", "indexmap"], workspace = true }
froglight-registry-template = { workspace = true }
variadics_please = { workspace = true }

# Optional dependencies
facet = { workspace = true, optional = true }
froglight-facet = { workspace = true, optional = true }

# --- Tests/Benchmarks ---

[[bench]]
name = "block"
required-features = ["std", "v26_1"]
harness = false

[[test]]
name = "attributes"
required-features = ["std"]

# --- Features ---

[features]
default = ["std"]

# Enable `facet` support.
facet = ["dep:facet", "froglight-common/facet", "dep:froglight-facet"]

# Enable `libm` for `no_std` support.
libm = ["froglight-common/libm"]
# Enable `once_cell` for `no_std` support.
once_cell = ["froglight-common/once_cell"]

# Enable support for the standard library
std = ["froglight-common/std", "froglight-facet?/std"]
# Enable optimizations using the nightly toolchain
nightly = ["froglight-common/nightly", "froglight-facet?/nightly"]

# -------------------------------------------------------------------------------------------------
# Note: The following features are automatically @generated.

v26_1 = ["froglight-common/v26_1"]
v26_2 = ["froglight-common/v26_2"]
