[default.extend-words]
# Fix modules starting with 'entitys2c' being split into 'entitys' and '2c'
entitys = "entitys"

[files]
extend-exclude = ["*.svg", "crates/froglight-registry/src/registries/*/**"]
